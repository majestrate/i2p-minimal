sourceSets {
    main {
        java {
            srcDir 'java/src'
        }
    }
    test {
        java {
            srcDir 'java/test/junit'
        }
    }
}

dependencies {
    compile project(':core')
    compile project(':ministreaming')
    testCompile project(path: ':ministreaming', configuration: 'tests')
}
